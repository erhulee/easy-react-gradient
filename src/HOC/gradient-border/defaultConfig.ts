import { borderConfig } from "./type";

export const defaultValue:borderConfig = {
    width:'2',
    colors:['(0,0,0,1)'],// rgb(123,323,453) 
    radius:false,
    radiusWidth: 0,
    direction:'vertical',
}